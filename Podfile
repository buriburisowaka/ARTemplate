# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'ARAppointment' do
  # Comment the next line if you don't want to use dynamic frameworks
  use_frameworks!

  # Pods for ARAppointment
  pod 'ARCore/Geospatial', '~> 1.33.0'

  target 'ARAppointmentTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'ARAppointmentUITests' do
    # Pods for testing
  end

end
